name: flutter_application_1
description: A new Flutter project.

publish_to: "none" # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  shared_preferences: 
  sqflite: 
  path_provider: 
# Ensure this line appears only once


dev_dependencies:
  flutter_lints: 

flutter:
  uses-material-design: true
